def return_netstat(strCommand, tv_sec, s):
	return tv_sec
	global hWorker
	print hWorker[strCommand]
	hWorker[strCommand]['EntryPoint'] = 'workerTypeI'
	hWorker[strCommand]['hCollection'] = [1, 2, 3]
	print hWorker[strCommand]
	return strCommand
	doneInfo = list()

	doneInfo.append(['SystemCPU', '%', 'Total', 25]);
	doneInfo.append(['SystemCPU', '%', 'free', 25]);

	return doneInfo
