def return_ps(strCommand, tv_sec, s):
	return tv_sec
	return strCommand
	doneInfo = list()

	doneInfo.append(['SystemCPU', '%', 'Total', 25]);
	doneInfo.append(['SystemCPU', '%', 'free', 25]);

	return doneInfo
